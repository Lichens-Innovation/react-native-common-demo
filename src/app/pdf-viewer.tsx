import { FunctionComponent } from 'react';
import PdfViewerScreen from '~/features/viewer/pdf-viewer-screen';

const PdfViewer: FunctionComponent = () => {
  return <PdfViewerScreen />;
};

export default PdfViewer;
